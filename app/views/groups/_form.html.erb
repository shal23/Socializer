<%= simple_form_for(@group) do |f| %>
  <%= f.error_notification %>

   <%= f.input :name, :input_html => { :class => "span4" } %>
    <%= f.input :description, :input_html => { :class => "span4", :rows => 3 }  %>

    <%= f.button :submit, :class => 'btn btn-success' %>

<% end %>
